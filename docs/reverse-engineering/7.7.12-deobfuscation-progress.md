# 7.7.12.js 反混淆进度与续作入口

> 更新时间：2026-09-16
>
> 原始文件 [scripts/7.7.12.js](../../scripts/7.7.12.js) 始终未修改。

## 当前最终输出

当前应优先使用：

- [scripts/7.7.12.deobfuscated.runtime-assisted.js](../../scripts/7.7.12.deobfuscated.runtime-assisted.js)
- 对应映射：[scripts/7.7.12.deobfuscated.runtime-assisted.js.map.json](../../scripts/7.7.12.deobfuscated.runtime-assisted.js.map.json)

这是第二轮 runtime-assisted 全文件输出：静态 AST 去混淆之后，又合并了 H5 runtime 中观察到的稳定 decoder/wrapper 返回值。

静态 baseline 仍保留，用于对比：

- [scripts/7.7.12.deobfuscated.final.js](../../scripts/7.7.12.deobfuscated.final.js)
- [scripts/7.7.12.deobfuscated.final.js.map.json](../../scripts/7.7.12.deobfuscated.final.js.map.json)

## 已完成工作

### 静态全文件 pass

- Acorn 解析整个文件并执行有限 AST 静态求值。
- 解码器 `a0D`、`a0w` 的声明、两层字符串表轮换和闭包 wrapper 已处理。
- 支持静态对象属性赋值、函数值转发、嵌套分支、函数参数遮蔽和 l-value 保护。
- 静态 baseline 完成约 `338,167` 项替换。
- 已恢复 `skip150_enabled`、`Configs`、`LevelConf.getById`、`hookInjected` 和 `monsters = [[[0]]]`。

### H5 runtime-assisted pass

- [public/game/push-level-research-bridge.js](../../public/game/push-level-research-bridge.js) 支持 `?wss-sandbox=1`：提供内存 WebSocket，阻断所有真实 WSS 出站。
- 雪花限制通过分块 tracer 解决：`scripts/7.7.12.runtime-traced-parts-256k/` 中的 56 个小脚本可任意顺序导入，最后一块自动拼接并执行。
- [tools/instrument-7.7.12-runtime.cjs](../../tools/instrument-7.7.12-runtime.cjs) 生成整文件 decoder/wrapper tracer。
- [tools/split-runtime-traced.cjs](../../tools/split-runtime-traced.cjs) 生成雪花分块。
- [tools/collect-runtime-trace.cjs](../../tools/collect-runtime-trace.cjs) 将浏览器 snapshot 保存到本地。
- 当前 trace：[tools/7.7.12.runtime-trace.json](../../tools/7.7.12.runtime-trace.json)。
- 当前保留 trace 捕获 `57,458,572` 次调用、`68,111` 个唯一值观测和 `44` 个 call-site 观测；`nativeSocketCount=0`。
- 当前 runtime-assisted 输出共 `338,877` 项替换，其中 `22,985` 项由稳定 runtime 观测支撑；对应 map 中记录了本次 trace 元数据。

## 继续工作命令

静态 pass：

```powershell
node tools/deobfuscate-7.7.12.cjs `
  scripts/7.7.12.js `
  scripts/7.7.12.deobfuscated.final.js
```

生成 tracer 和雪花分块：

```powershell
node tools/instrument-7.7.12-runtime.cjs `
  scripts/7.7.12.js `
  scripts/7.7.12.runtime-traced.js

node tools/split-runtime-traced.cjs `
  scripts/7.7.12.runtime-traced.js `
  scripts/7.7.12.runtime-traced-parts-256k `
  262144
```

浏览器页面使用：

```text
/game/index.html?wss-sandbox=1&deobfuscator-trace=1
```

收集 trace：

```powershell
node tools/collect-runtime-trace.cjs 4174 tools/7.7.12.runtime-trace.json
```

runtime-assisted pass：

```powershell
node tools/deobfuscate-7.7.12.cjs `
  scripts/7.7.12.js `
  scripts/7.7.12.deobfuscated.runtime-assisted.js `
  tools/7.7.12.runtime-trace.json
```

## 尚未完成

- 剩余 `a0D/a0w/vL/vS/qD/oo` 不等于全部失败调用，其中一部分是 decoder 定义、动态表轮换或无法稳定归因的调用。
- 同一源码调用点在不同字符串表状态/运行阶段可能返回多个值；当前只替换稳定观测，避免错误改写。
- 尚未完成混淆变量的语义重命名、业务模块拆分和全量行为等价测试。
- 当前 H5 sandbox 只提供 runtime，不模拟完整服务器响应；无账号运行时出现的 `sendAsync` 初始化异常不代表真实账号行为。

## 清理策略

保留原始脚本、两个最终输出、最终 map、trace、工具链和 256KB tracer 分块。旧版 v3-v10、debug 输出、qD 调试 dump、一次性运行日志和旧 32KB 分块包均可由工具重建，不作为续作依赖。